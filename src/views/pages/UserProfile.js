import React from 'react';
import PropTypes from 'prop-types';

const UserProfile = props => {
  return <div>User Profile: public view/ private view</div>;
};

UserProfile.propTypes = {};

export default UserProfile;
