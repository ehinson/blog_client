import React from 'react';
import PropTypes from 'prop-types';

const UserHome = props => {
  return (
    <div>
      User Home - has all user posts - on current_user posts can edit - maybe merge this with Home
      page
    </div>
  );
};

UserHome.propTypes = {};

export default UserHome;
